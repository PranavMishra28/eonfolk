# Resume EONFOLK V1

**Purpose:** Give a zero-context coordinator the exact restart while Release Genesis remains in the one V1 mega PR.

**Status:** FEATURE COMPLETE — exact-candidate DEEP PASS recorded; premerge evidence tail in progress

**Authority boundary:** [GOAL.md](GOAL.md) owns requirement state; this file owns the operational restart.

## Identity

- Branch: `feat/v1-civilization`
- Last exact app/CI integration checkpoint before the media evidence tail: `9540593f9a58f0a014fd9f4366e666a1a0b3729c`
- Hosted PR Verify at `e83a32b` failed 2 of 64 production journeys: the
  eleven-second Living World watch used a 45s cap that Linux world-open
  consumed, and abstention Chronicle used the default 5s visibility timeout
  instead of the 120s sponsor transition budget. Local FAST is green; the two
  journeys pass on this machine after the timeout repair.
- Starting `origin/main`: `8eb6afa911cbe386e18dddd26f093aaeef9e5167`
- Draft Mega PR: [#9](https://github.com/PranavMishra28/eonfolk/pull/9), `V1 INCOMPLETE: EONFOLK civilization mega PR`
- Machine: plugged-in Apple M4 Max with 48 GB unified memory
- Publication boundary: private, local-only, no deployment, spend, account, credential, or public release

Exact target-Mac DEEP intermediate run `32940785784` PASSed on frozen SHA `2462e38`. The PR remains draft until the evidence tail, eight receipts, and readiness checker pass. Product implementation is complete through the sponsor, Chronicle, accessible navigation, public export, SQLite/OPFS decision, twelve-actor presentation stress, reproducible media hooks, route auditing, and stable citizen/building/project camera-scale repairs integrated through `9540593`; do not mark it ready or merge until the exact FAST/PR/DEEP lattice and frozen-SHA reviews below pass.

## What is executable now

`/world` is the canonical Release Genesis product. A deterministic generated region advances through 1/7/30/90/365-day checkpoints and persists in versioned IndexedDB. It contains eight citizens across Dawnmere and the physically founded second settlement, conserved resources, needs, households, relationships, institutions, agreements, projects, migration, and a complete model-free Standard Brain.

The PlayCanvas world shows an inhabited physical settlement with recognizable stylized people, work, carrying, repair, conversation, props, paths, buildings, fields, project/growth context, and region/town/citizen camera scales. Contextual citizen/building/project selection works by pointer, keyboard, and typed deep link at desktop, laptop, and mobile sizes. The semantic world remains fully usable with reduced motion or renderer/asset failure.

The sponsor path is integrated into the generated authority: intervention or abstention produces an independent accept/reject/delay/reinterpretation, a distinct systemic consequence, durable reload/replay, allegation-versus-verification handling, and a causal-label-precise Chronicle. Chronicle links return to exact citizen, object, event, and site context without navigation or Reality mutation.

`/research` is a deliberate read-only evidence surface over the actual IndexedDB head, events, snapshots, sponsor covenant, causal parents, and Chronicle relations. The in-game feedback drawer sanitizes, previews, stores, reloads, and deletes reports only in the browser with explicit diagnostics consent and zero egress. Flight Recorder and Sentinel cover the required domains; all eight injected failure classes have closed browser outcomes.

Planning-era execution surfaces that were unreachable from V1 have been removed: the hosted feedback relay, obsolete Gate 0/Pixi harness, unused Observatory package, stale overnight handoff, and their active dependencies/config. Immutable tags and retained historical evidence preserve what actually ran.

## Current green evidence

- Exact clean FAST at `e2c47a2`: 466 unit tests and nine focused property tests.
- Exact clean PR execution at `de3f03b` completed the full unit, cognition,
  property, IndexedDB, timing, 11-fault, build, audit, and 64-journey browser
  sequence with zero egress; its wrapper then correctly refused an omitted
  `TLA2TOOLS_JAR`. The separately supplied pinned jar passed 52,379 generated /
  1,886 distinct states at depth 14. A fresh all-in-one exact candidate run is
  still required.
- Fourteen of fourteen targeted mutants killed.
- Eleven of eleven current injected-fault browser journeys.
- Focused exact-head Playwright passed four of four sponsor/accessibility/navigation journeys, including persisted manual reduced motion, a 44px Chronicle event target, and native Back restoring citizen focus after object activation.
- Real Chromium IndexedDB persistence passed two of two focused tests after the validated sponsor-session optimization.
- Typecheck, architecture, docs/Markdown, format, lint, build, bundle budget, dependency cohort, and production audit.
- Fresh in-app browser inspection at 1728×1117, 1366×768, and 390×844: world canvas ready, zero horizontal overflow, contextual mobile citizen focus visible.
- Current clean build/budget checkpoint: 75,542-byte critical shell and 664,801-byte initial-world JavaScript against the unchanged 204,800/665,600 limits.
- Fresh product media at `9540593` comes only from passing local Playwright
  journeys. Visual inspection caught and repaired the inherited 9 m camera that
  clipped Chronicle-to-building/project focus; both now restore explicit 24 m /
  28 m contextual scales.
- Clean twelve-actor presentation stress at the source-equivalent implementation checkpoint measured p95 8.5ms desktop, 9.2ms laptop, and 9.0ms mobile while retaining the stricter eight-citizen production budget. Final DEEP must bind a fresh exact-candidate result.

These are integration checks, not the final exact-candidate PR/DEEP/review evidence.

## Remaining sequence

1. Land the Linux CI timeout repair, finish exact clean FAST and PR lanes on
   that head, reconcile the execution ledger and performance record, then freeze
   the software candidate.
2. Run the complete target-Mac DEEP lane under the required ephemeral non-admin runner boundary and retain its exact evidence.
3. Run the six required independent reviews against the identical frozen SHA without cross-review exposure.
4. Reconcile and repair every P0/P1 once, run the targeted confirmation, write the V1 handoff, and create the clean evidence/documentation tail permitted by the readiness contract.
5. Mark Mega PR #9 ready only after the readiness checker and protected checks pass. Merge through protected `main`, run the required new push-to-main reattestation, then remove stale branches/worktrees whose unique heads are tag-reachable.

Do not restart architecture or product planning, add another PR, weaken the payload or authority gates, or substitute passing backend tests for the observable world.

## Restart commands

```bash
cd /Users/pranav/Documents/ChatGPT/metaverse
git fetch --all --tags --prune
git switch feat/v1-civilization
git status --short --branch
git log -1 --oneline
sed -n '1,190p' GOAL.md
pnpm verify:fast
```

If the branch head differs from the checkpoint above, inspect `git log --oneline e2c47a2..HEAD` and the actual diff before continuing. Never discard a dirty worktree or unique commit.
